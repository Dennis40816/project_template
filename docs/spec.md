# 產品規格

這是行為的唯一權威來源。其他文件引用這裡，不複製內容。保持短：寫「產品保證什麼」，不寫實作步驟。

## 1. 目的與使用者

<!-- 誰用、解決什麼問題、成功長什麼樣子。 -->

## 2. 功能

### 2.1 Inspect（範例）

- 輸入：一個檔案路徑。
- 輸出：檔案長度（bytes）與小寫 SHA-256。
- 錯誤：路徑空白 → `input.path-missing`；檔案不存在 → `input.file-not-found`；無法讀取 → `input.file-unreadable`。
- Desktop 與 CLI（`inspect <path>`）呼叫同一個 use case，輸出一致。
- CLI 結束碼：0 成功、1 有 issue、2 用法錯誤。

## 3. 不做的事

<!-- 明確列出範圍外的東西，避免之後被默默加進來。 -->

## 4. 相容性

<!-- 支援的 OS、檔案格式版本、設定檔遷移政策。 -->
